
# Acesso a Todos

To run the app, follow these steps.
## requirements

  ```shell
  npm install
  ```
  ```shell
  npm install -g gulp
  ```
  ```shell
  npm install -g jspm
  ```
## install dependencies

  ```shell
  jspm install -y
  ```

## run acesso a Todos
```shell
gulp watch
```
